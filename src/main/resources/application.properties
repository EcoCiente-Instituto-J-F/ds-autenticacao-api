spring.application.name=autenticacao

spring.datasource.url=${DB_URL:jdbc:postgresql://pg-3062d098-germinare-4a99.b.aivencloud.com:12525/defaultdb?sslmode=require}
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=${PORT:9801}

app.security.jwt.secret=${JWT_SECRET:dev-secret-key-mudar-em-producao-ecociente-dev-secret-key-12345}
app.security.jwt.expiration-minutes=${JWT_EXPIRATION_MINUTES:120}

spring.sql.init.mode=${SQL_INIT_MODE:always}
spring.jpa.defer-datasource-initialization=true