apiVersion: v1
kind: Service

metadata:
  name: ecociente-api-service
  namespace: ecociente

spec:
  selector:
    app: ecociente-api

  ports:
    - protocol: TCP
      port: 9801
      targetPort: 9801

  type: ClusterIP